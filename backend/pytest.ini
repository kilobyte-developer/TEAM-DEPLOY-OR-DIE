[pytest]
# Use importlib import mode to avoid "import file mismatch" errors
# when multiple test directories contain files with the same name
# (e.g. test_generated.py in each validation_exec_* directory).
# This works across all platforms: Linux, macOS, and Windows.
addopts = --import-mode=importlib
